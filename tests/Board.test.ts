import { Board } from '../src/Board'

describe('tests TemplateBuilder', () => {

    test('should have 3 columns, Activities, Complexities and Variations', () => {
        const board: Board = new Board()
        board
    })
})